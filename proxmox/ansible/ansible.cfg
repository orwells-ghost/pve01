[defaults]
inventory = inventory/
roles_path = ./roles
host_key_checking = True

# Use built-in callback with YAML formatting (replaces community.general.yaml)
stdout_callback = ansible.builtin.default
result_format = yaml
bin_ansible_callbacks = True